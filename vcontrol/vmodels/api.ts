// deno-lint-ignore-file
import {RouteParams} from './vlinks.ts';
// create an api to fetch safaricom bundles
// uses https /http to
interface clients{
    
}

export const paramsapi:any = (ctx:RouteParams)=>{
    console.log(ctx.request);
}

paramsapi

// chat room api start here using websocket
